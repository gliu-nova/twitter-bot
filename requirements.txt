requests>=2.32.0
yfinance>=0.2.40
python-dotenv>=1.0.0
PyYAML>=6.0.0
tweepy>=4.14.0
matplotlib>=3.8.0
httpx>=0.27.0
# On-chain bridges need market-memory with etherscan + blockscout packages.
# Local (recommended while developing both repos):
#   pip install -e ../market-memory
# Pinned remote (includes blockscout pipeline):
market-memory @ git+https://github.com/gliu-nova/market-memory.git@61b2c34
